package com.kiselev.time.service.calculator;

import com.kiselev.time.model.profile.Profile;

public class CalculatorService {

    public Long calculateTime(Profile profile, Integer price) {
        return 0L;
    }

    public Integer calculatePrice(Profile profile, Long time) {
        return 0;
    }
}
